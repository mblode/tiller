pre-commit:
  parallel: true
  jobs:
    - name: format-and-lint
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css}"
      run: npx oxfmt --write {staged_files} && cd apps/web && npx oxlint --fix .
      stage_fixed: true
